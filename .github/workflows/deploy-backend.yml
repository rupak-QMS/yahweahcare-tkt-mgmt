name: Deploy Backend

# ============================================================
# Deploys the backend to Azure App Service.
# Runs on push to main (backend changes only) or manual trigger.
#
# Required GitHub Secrets (Settings → Secrets → Actions):
#   AZURE_WEBAPP_PUBLISH_PROFILE — download from Azure Portal:
#     App Service → yahweahcare-api → Get publish profile
# ============================================================

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  AZURE_WEBAPP_NAME: yahweahcare-api
  NODE_VERSION: '18'

jobs:
  # ── Test gate ──────────────────────────────────────────────
  test:
    name: Run backend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Run tests
        working-directory: backend
        run: npm test -- --passWithNoTests

  # ── Deploy to Azure App Service ────────────────────────────
  deploy:
    name: Deploy to Azure App Service
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://yahweahcare-api.azurewebsites.net

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install production dependencies
        working-directory: backend
        run: npm ci --omit=dev

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: backend

      - name: Health probe
        run: |
          sleep 15
          curl --fail --retry 5 --retry-delay 5 \
            "https://yahweahcare-api.azurewebsites.net/api/health" && \
            echo "✓ /api/health responded OK"
